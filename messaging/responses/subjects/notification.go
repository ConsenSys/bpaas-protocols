package subjects

const (
	NOTIFICATION_INVITE_ACCEPT = "bpaas.notification.invite.accept"
	NOTIFICATION_INVITE        = "bpaas.notification.invite"
	NOTIFICATION_REJECT        = "bpaas.notification.invite.reject"
)
